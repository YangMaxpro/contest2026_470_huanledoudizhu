# Contest 2026 team 470 XiaoPai device service
if(CONFIG_LVX_USE_DEMO_CONTEST2026_470_XIAOPAI)
  set(XIAOPAI_SRCS xiaopai_main.c)
  if(CONFIG_XIAOPAI_WIFI_PERSIST)
    list(APPEND XIAOPAI_SRCS xiaopai_wifi.c)
  endif()
  if(CONFIG_XIAOPAI_VOICE)
    list(APPEND XIAOPAI_SRCS xiaopai_voice.c)
  endif()
  if(CONFIG_BK7258_AUDIO)
    list(APPEND XIAOPAI_SRCS xiaopai_audio.c)
  endif()
  if(CONFIG_XIAOPAI_MIMO)
    list(APPEND XIAOPAI_SRCS xiaopai_mimo.c)
  endif()
  if(CONFIG_XIAOPAI_NETWATCH)
    list(APPEND XIAOPAI_SRCS xiaopai_netwatch.c)
  endif()
  if(CONFIG_XIAOPAI_RNGCHECK)
    list(APPEND XIAOPAI_SRCS xiaopai_rngcheck.c)
  endif()
  if(CONFIG_XIAOPAI_HTTPCHECK)
    list(APPEND XIAOPAI_SRCS xiaopai_httpcheck.c)
  endif()
  if(CONFIG_XIAOPAI_HTTPSCHECK)
    list(APPEND XIAOPAI_SRCS xiaopai_tls.c)
  endif()
  nuttx_add_application(
    NAME xiaopai
    SRCS ${XIAOPAI_SRCS}
    STACKSIZE 8192)
  if(CONFIG_XIAOPAI_RTSA_PROBE)
    add_subdirectory(rtsa)
  endif()
endif()
